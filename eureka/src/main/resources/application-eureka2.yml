spring:
  profiles: eureka2
server:
  port: 8762
eureka:
  instance:
    hostname: sparrow-eureka2
  client:
    service-url:
    # eureka服务器的地址（注意：地址最后面的 /eureka/ 这个是固定值）
      default-zone: http://127.0.0.1:8761/eureka,http://127.0.0.1:8763/eureka